Reverse of string:

Code:

#include <stdio.h>

int main() {
  char string[1000], reverse[1000];
  int count=0,fact;
  printf("Enter string:");
  scanf(" %s", string);
  
  while(string[count] !='\0')
  {
      count++;
  }
  
  fact = count-1;
  for(int i=0 ;i< count; i++)
  {
      reverse[i] = string[fact];
      fact--;
  }
  
  printf("Reverse of string is %s",reverse);
    return 0;
}
