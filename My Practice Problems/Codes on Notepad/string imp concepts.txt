1) Take string input from user and print it.
fgets(name_of_sring; sizeof(string_name),stdin)
puts(name_of_sring)

Code:
#include<stdio.h>

int main()
{
char name[100];
printf("Enter string:");
fgets(name,sizeof(name),stdin);
printf("Name:");
puts(name);

return 0;
}

2) Passing string to function

Code:
#include<stdio.h>
void display(char str[])
{
    printf("");
    puts(str);
}
int main()
{
    char str[100];
    printf("Enter anything:");
    fgets(str, sizeof(str),stdin);
    display(str);
}